{
  "name": "@yuni/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "db:generate": "tsx scripts/with-root-env.ts prisma generate --schema prisma/schema.prisma",
    "db:migrate:dev": "tsx scripts/with-root-env.ts prisma migrate dev --schema prisma/schema.prisma",
    "db:migrate:deploy": "tsx scripts/with-root-env.ts prisma migrate deploy --schema prisma/schema.prisma",
    "db:seed": "tsx scripts/with-root-env.ts tsx prisma/seed.ts",
    "db:studio": "tsx scripts/with-root-env.ts prisma studio --schema prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "@yuni/config": "workspace:*",
    "@yuni/domain": "workspace:*"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  },
  "prisma": {
    "schema": "prisma/schema.prisma",
    "seed": "tsx prisma/seed.ts"
  }
}
